# Production environment overrides.
services:
  server:
    build:
      context: .
      target: prod
    ports:
      - "80:80" # HTTP
      - "443:443" # HTTPS
      - "443:443/udp" # HTTP/3
    environment:
      SERVER_NAME: ${SERVER_NAME:?Set to your production hostname}