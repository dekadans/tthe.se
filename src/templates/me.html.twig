<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>tthe.se/me</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/water.css@2/out/water.css">
    <style>
        table {
            table-layout: auto;
        }

        hr {
            margin: 3rem 0;
        }

        hgroup > h2,h3 {
            margin-bottom: 0;
        }

        hgroup > p {
            margin-top: 0;
        }

        ul.clean-list {
            list-style: none;
            padding-left: 0;
        }

        article {
            box-shadow: rgba(0, 0, 0, 0.05) 0px 6px 24px 0px, rgba(0, 0, 0, 0.08) 0px 0px 0px 1px;
            padding: 5px 1rem;
            margin: 1.5rem 0;
        }

        @media print {
            .no-print {
                display: none;
            }
        }
    </style>
</head>
<body>
{% import "macros.html.twig" as macros %}
<details class="no-print">
    <summary>What is this?</summary>
    <p>
        This is my master list of what I've done in my professional life, for the single purpose of having it all in one place.
    </p>
    <p>This document is generated from JSON-LD data available through <code>Accept: application/ld+json</code>.</p>
</details>

<ul class="clean-list">
    <li><strong>Name:</strong> {{ givenName }} {{ familyName }}</li>
    <li><strong>Location:</strong> {{ address.addressLocality }}, {{ address.addressCountry.name }}</li>
    <li><strong>Languages:</strong> {{ knowsLanguage|map(l => l.name)|join(', ') }}</li>
    <li><strong>Website:</strong> {{ macros.url(url) }}</li>
    <li><strong>Other links:</strong>
        <ul>
            {% for link in sameAs %}
                <li>{{ macros.url(link) }}</li>
            {% endfor %}
        </ul>
    </li>
</ul>

<hr>

<ol class="no-print">
    <li><a href="#work-history">Work History</a></li>
    <li><a href="#alma-mater">Almae Matres</a></li>
    <li><a href="#volunteering">Volunteering</a></li>
    <li><a href="#speaking">Public Speaking</a></li>
    <li><a href="#certifications">Professional Certifications</a></li>
    <li><a href="#credentials">Educational Credentials</a></li>
</ol>

<hr>

<h2 id="work-history">Work History</h2>
{% for role in worksFor %}
    {{ macros.role(role, "worksFor") }}
{% endfor %}
<hr>

<h2 id="alma-mater">Almae Matres</h2>
{% for role in alumniOf %}
    {{ macros.role(role, "alumniOf") }}
{% endfor %}
<hr>

<h2 id="volunteering">Volunteering</h2>
{% for role in affiliation %}
    {{ macros.role(role, "affiliation") }}
{% endfor %}
<hr>

{{ include('events.html.twig') }}

{{ include('certifications.html.twig') }}

{{ include('credentials.html.twig') }}

</body>
</html>